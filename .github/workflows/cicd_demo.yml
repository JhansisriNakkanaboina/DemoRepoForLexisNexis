name: cicd_demo

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build step
        run: echo "Build completed"

  ServiceNowDevOpsChange:
    needs: build
    runs-on: ubuntu-latest
    name: ServiceNow DevOps Change

    steps:
      - name: ServiceNow DevOps Change
        uses: ServiceNow/servicenow-devops-change@v5.1.0

        with:
          devops-integration-token: ${{ secrets.SN_DEVOPS_INTEGRATION_TOKEN }}
          instance-url: ${{ secrets.SN_INSTANCE_URL }}
          tool-id: ${{ secrets.SN_ORCHESTRATION_TOOL_ID }}
          context-github: ${{ toJSON(github) }}
          job-name: ServiceNow DevOps Change

          change-request: >
            {
              "attributes": {
                "requested_by": { "name": "Adam Ringle" },
                "short_description": "Short description from GitHub 28 april 26",
                "assignment_group": { "name": "ITSM Engineering" },
                "priority": "2",
                "comments": "This is a sample pipeline script to be added in your change step",
                "work_notes": "Update this to work_notes",
                "start_date": "2026-04-27 11:59:59",
                "end_date": "2026-04-29 11:59:59"
              }
            }
            
# Uncomment below if using deployment gates
#  deploy:
#    name: Deploy
#    needs: ServiceNowDevOpsChange
#    runs-on: ubuntu-latest
#    environment: deploy-gates-demo
#
#    steps:
#      - name: Run deployment scripts
#        run: echo "Completed Deployment"
